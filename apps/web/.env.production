# Committed on purpose, and it must stay empty.
#
# In production the API is served from the same origin under /api, which is what
# keeps the SameSite=Lax session cookie first-party. This file overrides any
# local .env so a production build can never bake in a dev URL.
VITE_API_URL=
