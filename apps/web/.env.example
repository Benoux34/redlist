# Local development only — copy to apps/web/.env.development
#
# Vite and the API run on different ports in dev, so the front needs the absolute
# origin. In production the value is forced empty by .env.production: the API is
# served from the same origin under /api.
VITE_API_URL="http://127.0.0.1:3000"
