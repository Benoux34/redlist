name: CI

on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: ci
          POSTGRES_PASSWORD: ci
          POSTGRES_DB: ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://ci:ci@localhost:5432/ci

    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2

      - run: bun install --frozen-lockfile

      - run: bunx prisma generate
        working-directory: apps/api

      - run: bunx tsc --noEmit -p packages/contracts/tsconfig.json
      - run: bunx tsc --noEmit -p apps/api/tsconfig.json
      - run: bunx tsc --noEmit -p apps/web/tsconfig.json
      - run: bunx tsc --noEmit

      - run: bunx oxlint --type-aware

      - run: bun test

      - name: Apply migrations
        run: bunx prisma migrate deploy
        working-directory: apps/api

      - name: Check migrations match the schema
        run: |
          bunx prisma migrate diff \
            --from-config-datasource \
            --to-schema prisma/schema \
            --exit-code
        working-directory: apps/api

      - run: bun run build
        working-directory: apps/web

  images:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v3

      - name: Build API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/api/Dockerfile
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # deploy:
  # needs: [check, images]
  # if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  # runs-on: ubuntu-latest

  # concurrency:
  #   group: deploy-production
  #   cancel-in-progress: false

  # steps:
  #   - name: Deploy over SSH
  #     env:
  #       SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
  #       KNOWN_HOSTS: ${{ secrets.DEPLOY_KNOWN_HOSTS }}
  #       TARGET: ${{ secrets.DEPLOY_TARGET }}
  #     run: |
  #       set -euo pipefail

  #       # An unset secret arrives as an empty string, and the failure that
  #       # follows is "Host key verification failed" -- which points at the
  #       # wrong thing entirely.
  #       for name in SSH_KEY KNOWN_HOSTS TARGET; do
  #         if [ -z "${!name:-}" ]; then
  #           echo "!! Secret DEPLOY_$name is empty or missing." >&2
  #           exit 1
  #         fi
  #       done

  #       mkdir -p ~/.ssh && chmod 700 ~/.ssh
  #       printf '%s\n' "$SSH_KEY" > ~/.ssh/id_ed25519
  #       chmod 600 ~/.ssh/id_ed25519
  #       # Pinned rather than discovered with ssh-keyscan, which would trust
  #       # whatever answers at deploy time.
  #       printf '%s\n' "$KNOWN_HOSTS" > ~/.ssh/known_hosts
  #       # No command: the key is restricted to deploy.sh in authorized_keys,
  #       # so a leak cannot open a shell.
  #       ssh -i ~/.ssh/id_ed25519 "$TARGET"
